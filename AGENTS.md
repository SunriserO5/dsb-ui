# Project Guide

## 项目定位

本项目用于搭建直播推流前端 UI。核心输出是给 OBS、直播伴侣或其他采集软件使用的高画质直播画面，展示选手屏幕共享、选手信息、队伍信息、赛况信息，以及可切换的暂停/待机画面。

参考图位于 `eg/`：

- `eg/直播界面-比赛版本.png`：比赛直播主画面参考。
- `eg/待机界面-1.png`：待机/暂停画面参考。

## 关键原则

- 画质优先：直播输出应优先保证 1920x1080、16:9、稳定帧率、清晰文本和干净边缘。
- 控制面板必须和直播采集画面隔离：控制面板不能出现在被 OBS/直播软件采集的窗口、路由或 DOM 区域内。
- 直播输出页只包含观众可见内容，不放调试控件、编辑控件、状态面板或鼠标操作提示。
- 控制面板应通过单独页面、单独窗口或外部设备访问，用于切换比赛画面、暂停画面、选手信息和其他状态。
- 所有画面状态尽量数据驱动，避免为了改选手、队伍、标题、计时等内容去改布局代码。

## 推荐页面结构

- `/live` 或等价输出页：纯直播采集画面，固定 16:9 画布，默认按 1920x1080 设计。
- `/standby` 或作为 `/live` 的 scene：暂停、待机、转场等待画面。
- `/control`：控制面板，不能被采集；用于切换 scene、编辑选手/队伍/赛况、控制计时和显示状态。

如果使用单页应用，也要在视觉和 DOM 上保持采集区与控制区完全分离。优先采用独立窗口或独立浏览器标签页，让采集软件只采集直播输出页。

## 直播画面要求

- 主区域用于选手屏幕共享，应尽量减少遮挡，装饰层不要压住关键内容。
- 右侧信息栏展示当前选手、所属队伍、队伍成员或出场顺序等信息。
- 底部信息区可展示本场解说、支持用时、赛事提示或滚动信息。
- 暂停/待机画面应可一键切换，且不依赖重新加载页面。
- 对齐、尺寸和留白要固定，避免数据变化导致布局跳动。
- 文字必须在直播压缩后仍清楚可读，避免过细字重、小字号和低对比度。

## 性能与画质

- 避免在选手屏幕共享区域上叠加昂贵滤镜、大面积模糊或高频透明动画。
- 动画应克制且稳定，优先使用 transform/opacity，避免持续触发布局。
- 固定输出画布比例，使用响应式缩放适配窗口，但内部设计基准保持 1920x1080。
- 图片资源应使用足够分辨率，避免采集后放大导致糊边。
- 字体资源应本地化或随项目打包，避免直播现场网络波动影响渲染。

## 控制面板要求

- 控制面板应服务导播/操作员，追求可靠、快速、低误触。
- 应提供明确的当前 scene 状态，例如比赛中、暂停、待机。
- 场景切换要有确认反馈，关键操作可考虑防误触设计。
- 控制面板样式不需要和直播输出完全一致，但需要清晰、密集、可扫描。

## 开发注意事项

- 新增功能前先确认它属于观众可见直播输出，还是只属于控制面板。
- 不要把控制按钮、输入框、调试信息、开发水印放进直播输出页。
- 修改布局时优先在 1920x1080 下检查，再兼顾其他窗口尺寸。
- 如果引入第三方依赖，先检查项目现有 `package.json`，保持技术栈一致。
- 前端视觉实现应贴近参考图的国风赛事包装方向，但不要牺牲屏幕共享区域清晰度。

